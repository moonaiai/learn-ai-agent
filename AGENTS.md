# Project Agent Instructions

When writing or editing repository documents, read `agents/writing-skill/SKILL.md` first and follow its linked style guide.

Keep generated documentation in Chinese by default unless the requested artifact is explicitly English.

Use local image assets with relative Markdown paths. Do not depend on remote image URLs for document body images.
